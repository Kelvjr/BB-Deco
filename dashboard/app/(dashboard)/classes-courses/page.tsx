import { WorkspaceShell } from "@/components/workspace-shell";

export default function ClassesCoursesPage() {
  return (
    <WorkspaceShell
      title="Classes & courses"
      subtitle="Curriculum, cohorts, and schedules."
    />
  );
}
