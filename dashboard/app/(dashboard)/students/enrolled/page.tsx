import { WorkspaceShell } from "@/components/workspace-shell";

export default function StudentsEnrolledPage() {
  return (
    <WorkspaceShell
      title="Enrolled students"
      subtitle="Currently active in a program."
    />
  );
}
